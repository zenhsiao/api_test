	<% if current_user.liked_event?(event) %>
    <%= button_to 'unlike', like_event_path(event), :remote =>true %>		
	<% else %>
    <%= button_to 'like', like_event_path(event), :remote => true %>
	<% end %>